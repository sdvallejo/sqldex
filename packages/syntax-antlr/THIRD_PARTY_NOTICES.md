# Third-party notices

## `src/generated/`

`src/generated/MySQLLexer.ts`, `src/generated/MySQLParser.ts`, and
`src/generated/MySQLParserListener.ts` are generated by the ANTLR4 tool from a grammar vendored
from [`antlr/grammars-v4`](https://github.com/antlr/grammars-v4), `sql/mysql/Oracle/` —
`MySQLLexer.g4` and `MySQLParser.g4`, at the commit pinned in `src/generated/VENDORED_GRAMMAR_COMMIT.ts`.

That grammar carries its own license:

```
Copyright © 2025, Oracle and/or its affiliates
Licensed under the BSD 3-Clause License: https://opensource.org/license/bsd-3-clause
```

`src/generated/MySQLLexerBase.ts`, `src/generated/MySQLParserBase.ts`, `src/generated/SqlMode.ts`
and `src/generated/SqlModes.ts` are copied unmodified from the same grammar's `Antlr4ng/` target
directory (support code the generated lexer/parser require), under the same license.

BSD-3-Clause is a permissive license compatible with this package's own MIT license; this notice
exists to carry the required attribution forward, not because of any incompatibility.

## Regeneration

See `tools/regenerate.sh` for how these files are produced. They are vendored (committed), never
generated at `npm install` time — regenerating against a newer upstream grammar release is a
deliberate, manual, maintainer action.
